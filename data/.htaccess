# data/schedule.php is a config file, not a page. It is require()d from PHP, so
# it never needs to be reachable over HTTP. Deny it outright.
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order allow,deny
    Deny from all
</IfModule>
